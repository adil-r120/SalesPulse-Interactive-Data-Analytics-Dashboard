// PostCSS configuration file for the SalesPulse application
// Configures PostCSS plugins for processing CSS files
// Integrates Tailwind CSS and Autoprefixer for modern CSS features

// Export the PostCSS configuration
export default {
  // Plugin configuration
  plugins: {
    // Tailwind CSS plugin for utility-first CSS framework
    tailwindcss: {},
    // Autoprefixer plugin for adding vendor prefixes to CSS rules
    autoprefixer: {},
  },
}